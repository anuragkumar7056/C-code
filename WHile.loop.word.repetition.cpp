#include<iostream>
using namespace std;
int main()
{
 vector<int> v;
int i; // read until end-of-file or other input failure
while (cin >> i)
v.push_back(i); // find the first negative element
auto beg = v.begin();
while (beg != v.end() && beg==beg+1)
{string *beg;
int [*beg]=1;
[*beg]++;
if
++beg;
}
if (beg == v.end())
}
